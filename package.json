{
  "name": "poster-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Extract poster image URLs from IMDb pages and optionally reupload them to Imgbox.",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
